---
# defaults file for role-ssh
# SSH package name
ssh_package_name: openssh-server
ssh_restart_handler_state: restarted
ssh_package_dependencies:
  - figlet
ssh_custom_banner: false
ssh_custom_banner_path: "/etc/ssh"
ssh_custom_banner_file_name: "banner"
ssh_custom_banner_welcome_text: "asaplab"

ssh_user_root:
  - name: root
    home_dir: "/"
    manage_ssh_key: true
    ssh_key_type: rsa
    ssh_key_size: 2048
    private_key: ""
    public_key: ""
    authorized_keys:
      - key: "{{ lookup('file', lookup('env', 'HOME') + '/.ssh/id_rsa.pub') }}"
        state: present

# SSH users setup
ssh_user_users:
  []

# SSH secure configuration
ssh_secure_config: false

# SSH secure config custom if true
ssh_secure_config_custom:
  []

---
# defaults file for role-ssh
ssh_user_root:
  []
  # - name: root
  #   home_dir: "/"
  #   manage_ssh_key: yes
  #   private_key: ""
  #   authorized_keys:
  #     - key: "{{ lookup('file', lookup('env','HOME') + '/.ssh/id_rsa.pub') }}"
  #       state: present

ssh_user_users:
  []
  # - name: ""
  #   manage_ssh_key: yes
  #   private_key: ""
  #   authorized_keys:
  #     - key: "ssh-rsa ABC123"
  #       state: present
  #     - key: "ssh-rsa ABC123456"
  #       state: present
